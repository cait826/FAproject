<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Approve Refund | <%= ticket.id %></title>
    <style>
      :root {
        --bg: #fef6e4;
        --card: #ffffff;
        --accent: #ff9f1c;
        --text: #172b4d;
        --muted: #5f6c7b;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, sans-serif;
        background: radial-gradient(circle at 18% 18%, #ffd6a5 0, rgba(255,214,165,0) 32%), radial-gradient(circle at 78% 0%, #a0e8af 0, rgba(160,232,175,0) 32%), var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 72px 16px 64px;
      }
      .card {
        width: min(640px, 100%);
        background: var(--card);
        border: 1px solid rgba(0,0,0,0.06);
        border-radius: 18px;
        padding: 22px;
        box-shadow: 0 18px 36px rgba(0,0,0,0.12);
      }
      h1 { margin: 0 0 8px; }
      p { margin: 0 0 12px; color: var(--muted); }
      form { display: grid; gap: 12px; }
      label { display: flex; flex-direction: column; gap: 6px; color: var(--muted); font-weight: 600; }
      select, input[type="checkbox"] { accent-color: var(--accent); }
      select {
        padding: 10px 12px;
        border-radius: 12px;
        border: 1px solid rgba(0,0,0,0.12);
        background: #fefaf2;
        color: var(--text);
      }
      button {
        background: var(--accent);
        color: #0f172a;
        border: none;
        border-radius: 12px;
        padding: 12px 14px;
        font-weight: 700;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="card">
      <h1>Approve Ticket <%= ticket.id %></h1>
      <p>Order: <%= ticket.orderId %> · Customer Wallet: <%= ticket.customer %></p>
      <form method="post" action="/admin/customer-service/approve/<%= ticket.id %>">
        <label>Refund Type
          <select name="refundType" id="refundType" required>
            <option value="">Select type</option>
            <option value="Partial">Partial</option>
            <option value="Full">Full</option>
          </select>
        </label>
        <label id="newItemWrap" style="display:none; flex-direction:row; gap:8px; align-items:center; color:var(--text);">
          <input type="checkbox" name="requestNewItem" value="on" />
          <span>Request for New Item</span>
        </label>
        <button type="submit">Approve</button>
      </form>
    </div>
    <%- include('partials/footer') %>
    <script>
      const refundType = document.getElementById('refundType');
      const wrap = document.getElementById('newItemWrap');
      const toggle = () => {
        wrap.style.display = refundType.value === 'Partial' ? 'flex' : 'none';
      };
      refundType.addEventListener('change', toggle);
      toggle();
    </script>
  </body>
</html>
