<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Customer Service | Republic Surprise</title>
    <style>
      :root {
        --bg: #fef6e4;
        --card: #ffffff;
        --accent: #ff9f1c;
        --accent-2: #2ec4b6;
        --text: #172b4d;
        --muted: #5f6c7b;
        --line: rgba(0,0,0,0.06);
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, sans-serif;
        background: radial-gradient(circle at 18% 18%, #ffd6a5 0, rgba(255,214,165,0) 32%), radial-gradient(circle at 78% 0%, #a0e8af 0, rgba(160,232,175,0) 32%), var(--bg);
        color: var(--text);
        min-height: 100vh;
        padding: 72px 16px 64px;
      }
      .back-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 12px;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.9);
        color: var(--text);
        text-decoration: none;
        font-weight: 700;
        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.12);
      }
      .top-row {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 18px;
      }
      .back-link span {
        font-size: 1.1rem;
        line-height: 1;
      }
      .page { max-width: 1200px; margin: 0 auto; }
      .card {
        background: var(--card);
        border: 1px solid var(--line);
        border-radius: 18px;
        padding: 22px;
        box-shadow: 0 18px 36px rgba(0,0,0,0.12);
        overflow-x: auto;
      }
      h1 { margin: 0 0 10px; letter-spacing: 0.4px; }
      p { margin: 0 0 16px; color: var(--muted); }
      table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 10px;
        min-width: 1100px;
      }
      th, td {
        text-align: left;
        padding: 12px 14px;
        background: #fefaf2;
        border: 1px solid var(--line);
      }
      th { background: #ffe8c9; }
      .action {
        border: none;
        background: var(--accent);
        color: #0f172a;
        border-radius: 10px;
        padding: 10px 12px;
        font-weight: 700;
        cursor: pointer;
      }
      .action:disabled { background: #e2e8f0; color: #6b7280; cursor: not-allowed; }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 6px 10px;
        border-radius: 999px;
        font-weight: 700;
      }
      .pill.open { background: rgba(255,159,28,0.15); color: #b45309; }
      .pill.review { background: rgba(46,196,182,0.18); color: #0b3b35; }
      .pill.approved { background: rgba(34,197,94,0.2); color: #14532d; }
      header, footer {
        position: fixed;
        left: 0;
        right: 0;
        padding: 14px 18px;
        text-align: center;
        background: rgba(255,255,255,0.7);
        backdrop-filter: blur(6px);
        color: var(--text);
      }
      header { top: 0; }
      footer { bottom: 0; }
      header h2, footer p { margin: 0; letter-spacing: 0.5px; }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="page">
      <div class="top-row">
        <a class="back-link" href="/admin/dashboard"><span>&larr;</span> Back</a>
      </div>
      <div class="card">
        <h1>Customer Service</h1>
        <p>Manage partial/full refunds and customer support tickets.</p>
        <% if (tickets && tickets.length) { %>
          <table>
            <thead>
              <tr>
                <th>Ticket ID</th>
                <th>Order ID</th>
                <th>Customer Wallet</th>
                <th>Reason</th>
                <th>Amount</th>
                <th>Type</th>
                <th>Status</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <% tickets.forEach(function(t) { %>
                <tr>
                  <td><a href="/admin/customer-service/<%= t.id %>"><%= t.id %></a></td>
                  <td><%= t.orderId %></td>
                  <td><%= t.customer %></td>
                  <td><%= t.reason || '-' %></td>
                  <td>$<%= Number(t.amount || 0).toFixed(2) %></td>
                  <td><%= t.type %> refund</td>
                  <td>
                    <% const pillClass = t.status === 'Open' ? 'open' : t.status === 'Approved' ? 'approved' : 'review'; %>
                    <span class="pill <%= pillClass %>"><%= t.status %></span>
                  </td>
                  <td>
                    <form method="get" action="/admin/customer-service/approve/<%= t.id %>">
                      <button class="action" type="submit" <%= t.status === 'Approved' ? 'disabled' : '' %>>Approve</button>
                    </form>
                    <form method="post" action="/admin/customer-service/reject/<%= t.id %>" style="margin-top:6px;">
                      <button class="action" type="submit" <%= t.status === 'Rejected' ? 'disabled' : '' %>>Reject</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <p>No customer service tickets yet.</p>
        <% } %>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
