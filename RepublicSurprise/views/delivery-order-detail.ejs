<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delivery Order <%= delivery.id %></title>
    <style>
      :root {
        --bg: #fef6e4;
        --card: #ffffff;
        --accent: #ff9f1c;
        --text: #172b4d;
        --muted: #5f6c7b;
        --line: rgba(0,0,0,0.08);
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, sans-serif;
        background: radial-gradient(circle at 18% 18%, #ffd6a5 0, rgba(255,214,165,0) 32%), radial-gradient(circle at 78% 0%, #a0e8af 0, rgba(160,232,175,0) 32%), var(--bg);
        color: var(--text);
        min-height: 100vh;
        padding: 72px 16px 64px;
      }
      .page { max-width: 720px; margin: 0 auto; }
      .card {
        background: var(--card);
        border: 1px solid var(--line);
        border-radius: 18px;
        padding: 22px;
        box-shadow: 0 18px 36px rgba(0,0,0,0.12);
      }
      h1 { margin: 0 0 10px; letter-spacing: 0.4px; }
      p { margin: 0 0 10px; color: var(--muted); }
      .row { display: grid; grid-template-columns: 160px 1fr; gap: 10px; margin-bottom: 8px; }
      .label { font-weight: 700; }
      header, footer {
        position: fixed;
        left: 0;
        right: 0;
        padding: 14px 18px;
        text-align: center;
        background: rgba(255,255,255,0.7);
        backdrop-filter: blur(6px);
        color: var(--text);
      }
      header { top: 0; }
      footer { bottom: 0; }
      header h2, footer p { margin: 0; letter-spacing: 0.5px; }
      .actions { margin-top: 14px; }
      .action {
        border: none;
        background: var(--accent);
        color: #0f172a;
        border-radius: 10px;
        padding: 10px 12px;
        font-weight: 700;
        cursor: pointer;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="page card">
      <h1>Order <%= delivery.id %></h1>
      <p>Delivery order details.</p>
      <div class="row"><div class="label">Customer full name</div><div><%= delivery.customerName || delivery.fullName || delivery.customer || 'Guest' %></div></div>
      <div class="row"><div class="label">Order Number</div><div><%= delivery.orderNumber || delivery.id %></div></div>
      <div class="row"><div class="label">Address</div><div><%= delivery.address || 'N/A' %></div></div>
      <div class="row"><div class="label">Contact Number</div><div><%= delivery.contact || 'N/A' %></div></div>
      <div class="row"><div class="label">Status</div><div><%= delivery.status %></div></div>
      <div class="actions">
        <% if (user && user.role === 'delivery man') { %>
          <a class="action" href="/delivery/dashboard">Back</a>
        <% } else if (user && user.role === 'admin') { %>
          <a class="action" href="/admin/orders">Back</a>
        <% } else { %>
          <a class="action" href="/">Back</a>
        <% } %>
      </div>

      <% if (user && user.role === 'delivery man' && delivery.assignedTo === user.walletAddress) { %>
        <hr />
        <h3>Submit Proof of Delivery</h3>
        <% if (delivery.proofImage && delivery.proofImage.data) { %>
          <div style="margin-bottom:8px;">Existing proof:</div>
          <div style="margin-bottom:12px;"><img src="data:<%= delivery.proofImage.mimetype %>;base64,<%= delivery.proofImage.data %>" style="max-width:320px;border:1px solid #eee;padding:6px;border-radius:8px;"/></div>
        <% } %>
        <form method="POST" action="/deliveries/<%= delivery.id %>/submit-proof" enctype="multipart/form-data">
          <div style="margin-bottom:8px;"><label>Photo (required): <input type="file" name="proofImage" accept="image/*" required></label></div>
          <div style="margin-bottom:8px;"><label>Remarks: <br><textarea name="remarks" rows="3" style="width:100%"><%= delivery.remarks || '' %></textarea></label></div>
          <div style="margin-bottom:8px;"><label>Signature (optional): <input type="text" name="signature" value="<%= delivery.signature || '' %>"></label></div>
          <div><button type="submit" class="action">Submit Proof</button></div>
        </form>
      <% } %>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
