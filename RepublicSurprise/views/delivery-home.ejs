<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delivery Dashboard | Republic Surprise</title>
    <style>
      :root {
        --bg: #fef6e4;
        --card: #ffffff;
        --accent: #ff9f1c;
        --accent-2: #2ec4b6;
        --text: #172b4d;
        --muted: #5f6c7b;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, sans-serif;
        background: radial-gradient(circle at 18% 18%, #ffd6a5 0, rgba(255,214,165,0) 32%), radial-gradient(circle at 78% 0%, #a0e8af 0, rgba(160,232,175,0) 32%), var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 72px 16px 64px;
        text-align: center;
      }
      .card {
        width: min(720px, 100%);
        background: var(--card);
        border: 1px solid rgba(0,0,0,0.05);
        border-radius: 18px;
        padding: 28px;
        box-shadow: 0 18px 36px rgba(0,0,0,0.12);
      }
      h1 { margin: 0 0 10px; letter-spacing: 0.5px; }
      p { margin: 0 0 16px; color: var(--muted); }
      .links {
        display: flex;
        gap: 12px;
        justify-content: center;
        flex-wrap: wrap;
        margin-top: 12px;
      }
      a.button {
        background: var(--accent);
        color: #0f172a;
        border: none;
        border-radius: 12px;
        padding: 14px 18px;
        font-weight: 700;
        cursor: pointer;
        text-decoration: none;
        transition: transform 0.15s ease, box-shadow 0.2s ease;
      }
      a.button:hover { transform: translateY(-2px); box-shadow: 0 12px 24px rgba(255,159,28,0.35); }
      a.button.alt { background: var(--accent-2); color: #0b3b35; }
      table.delivery-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 14px;
        margin-top: 20px;
      }
      table.delivery-table td {
        text-align: left;
        padding: 14px 16px;
        background: #fefaf2;
        border: 1px solid rgba(0,0,0,0.05);
        border-radius: 14px;
      }
      table.delivery-table a.button {
        padding: 12px 14px;
        display: inline-block;
        margin-right: 6px;
      }
      header, footer {
        position: fixed;
        left: 0;
        right: 0;
        padding: 14px 18px;
        text-align: center;
        background: rgba(255,255,255,0.7);
        backdrop-filter: blur(6px);
        color: var(--text);
      }
      header { top: 0; }
      footer { bottom: 0; }
      header h2, footer p { margin: 0; letter-spacing: 0.5px; }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
<div class="card">
  <h1>Delivery Dashboard</h1>
  <p>Hello, <strong><%= deliveryName %></strong>!</p>

  <div style="display:flex; gap:12px; justify-content:center; margin-top:12px;">
    <div style="background:#fff; padding:12px 16px; border-radius:10px; border:1px solid rgba(0,0,0,0.04);">
      <div style="font-weight:700">Assigned</div>
      <div style="font-size:20px"><%= stats && stats.assigned ? stats.assigned : 0 %></div>
    </div>
    <div style="background:#fff; padding:12px 16px; border-radius:10px; border:1px solid rgba(0,0,0,0.04);">
      <div style="font-weight:700">Pending</div>
      <div style="font-size:20px"><%= stats && stats.pending ? stats.pending : 0 %></div>
    </div>
    <div style="background:#fff; padding:12px 16px; border-radius:10px; border:1px solid rgba(0,0,0,0.04);">
      <div style="font-weight:700">Delivered (Pending)</div>
      <div style="font-size:20px"><%= stats && stats.delivered_pending ? stats.delivered_pending : 0 %></div>
    </div>
  </div>

  <% if (deliveries && deliveries.length) { %>
    <table class="delivery-table">
      <thead>
        <tr>
          <td><strong>Order ID</strong></td>
          <td><strong>Delivery Address</strong></td>
          <td><strong>Status</strong></td>
          <td><strong>Action</strong></td>
        </tr>
      </thead>
      <tbody>
        <% deliveries.forEach(function(d){ %>
          <tr>
            <td><a href="/deliveries/<%= d.id %>"><%= d.orderNumber %></a></td>
            <td><%= d.address || 'N/A' %></td>
            <td>
              <% let label = d.status || ''; %>
              <% if (label === 'out_for_delivery') { label = 'Out for Delivery'; } %>
              <% if (label === 'delivered_pending') { label = 'Delivered (Pending)'; } %>
              <%= label %>
            </td>
            <td>
              <% if (d.status === 'out_for_delivery') { %>
                <a class="button" href="/deliveries/<%= d.id %>">Deliver</a>
              <% } else if (d.status === 'delivered_pending') { %>
                <a class="button" href="/deliveries/<%= d.id %>">View Proof</a>
              <% } else { %>
                <a class="button" href="/deliveries/<%= d.id %>">View</a>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } else { %>
    <p>No assigned deliveries.</p>
  <% } %>

  <div class="links" style="margin-top:20px;">
    <a class="button alt" href="/delivery-history">Delivery History</a>
  </div>
</div>
<%- include('partials/footer') %>
  </body>
</html>
