<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order <%= order.id %> | Admin</title>
    <style>
      :root {
        --bg: #fef6e4;
        --card: #ffffff;
        --accent: #ff9f1c;
        --text: #172b4d;
        --muted: #5f6c7b;
        --line: rgba(0,0,0,0.08);
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: "Segoe UI", Tahoma, sans-serif;
        background: radial-gradient(circle at 18% 18%, #ffd6a5 0, rgba(255,214,165,0) 32%), radial-gradient(circle at 78% 0%, #a0e8af 0, rgba(160,232,175,0) 32%), var(--bg);
        color: var(--text);
        min-height: 100vh;
        padding: 72px 16px 64px;
      }
      .page {
        max-width: 760px;
        margin: 0 auto;
      }
      .card {
        background: var(--card);
        border: 1px solid var(--line);
        border-radius: 18px;
        padding: 22px;
        box-shadow: 0 18px 36px rgba(0,0,0,0.12);
      }
      h1 { margin: 0 0 10px; letter-spacing: 0.4px; }
      p { margin: 0 0 10px; color: var(--muted); }
      .row { display: grid; grid-template-columns: 160px 1fr; gap: 10px; margin-bottom: 8px; }
      .label { font-weight: 700; color: var(--text); }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 6px 10px;
        border-radius: 999px;
        font-weight: 700;
      }
      .pill.pending { background: rgba(255,159,28,0.15); color: #b45309; }
      .pill.confirmed { background: rgba(46,196,182,0.18); color: #0b3b35; }
      header, footer {
        position: fixed;
        left: 0;
        right: 0;
        padding: 14px 18px;
        text-align: center;
        background: rgba(255,255,255,0.7);
        backdrop-filter: blur(6px);
        color: var(--text);
      }
      header { top: 0; }
      footer { bottom: 0; }
      header h2, footer p { margin: 0; letter-spacing: 0.5px; }
      .actions { margin-top: 14px; display: flex; gap: 10px; }
      .action {
        border: none;
        background: var(--accent);
        color: #0f172a;
        border-radius: 10px;
        padding: 10px 12px;
        font-weight: 700;
        cursor: pointer;
      }
      .action:disabled {
        background: #e2e8f0;
        color: #6b7280;
        cursor: not-allowed;
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="page card">
      <h1>Order <%= order.id %></h1>
      <p>Order details and delivery confirmation.</p>
      <div class="row"><div class="label">Product</div><div><%= order.product %></div></div>
      <div class="row"><div class="label">Customer Wallet</div><div><%= order.customer %></div></div>
      <div class="row"><div class="label">Price</div><div>$<%= Number(order.price || 0).toFixed(2) %></div></div>
      <div class="row"><div class="label">Quantity</div><div><%= order.qty %></div></div>
      <div class="row"><div class="label">Status</div>
        <div><span class="pill <%= order.status === 'Confirmed' || order.status === 'Completed' ? 'confirmed' : 'pending' %>"><%= order.status %></span></div>
      </div>
      <div class="actions">
        <a class="action" style="text-decoration:none; display:inline-flex; align-items:center;" href="/admin/orders">Back to Orders</a>
      </div>
      <hr style="margin:18px 0; border: none; border-top:1px solid rgba(0,0,0,0.08);" />
      <% if (ticket) { %>
        <h2 style="margin:0 0 8px;">Refund request</h2>
        <div class="row"><div class="label">Ticket ID</div><div><%= ticket.id %></div></div>
        <div class="row"><div class="label">Reason</div><div><%= ticket.reason || '-' %></div></div>
        <div class="row"><div class="label">Status</div><div><%= ticket.status || '-' %></div></div>
        <% if (ticket.description) { %>
          <div class="row"><div class="label">Details</div><div><%= ticket.description %></div></div>
        <% } %>
        <% if (ticket.resolvedAt) { %>
          <div class="row"><div class="label">Resolved</div><div><%= ticket.resolvedAt %></div></div>
        <% } %>
        <hr style="margin:18px 0; border: none; border-top:1px solid rgba(0,0,0,0.08);" />
      <% } %>
      <h2 style="margin:0 0 8px;">Audit log</h2>
      <% const audits = order.auditLog || order.audits || []; %>
      <% if (!audits.length) { %>
        <p style="color:#5f6c7b;">No audit entries yet.</p>
      <% } else { %>
        <ul style="list-style:disc; padding-left:18px; margin:0; color:#172b4d;">
          <% audits.forEach(function(a) { %>
            <li>
              <strong><%= a.action %></strong>
              <span style="color:#5f6c7b;">@ <%= a.timestamp %></span>
              <div style="font-size:0.9rem; color:#172b4d; margin-top:2px;">
                Function: <%= a.function || 'N/A' %><br/>
                Tx: <%= a.txHash || 'N/A' %>
              </div>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
